"""policy_eval — functional test harness for layered policy composition.

NOTE on naming: parent plan + RFC vernacular uses `policy-eval` (hyphen). Python
package names can't contain hyphens, so the importable module is `policy_eval`
(underscore). Verdict tape and report paths still use the hyphenated form
externally for consistency with the plan."""
